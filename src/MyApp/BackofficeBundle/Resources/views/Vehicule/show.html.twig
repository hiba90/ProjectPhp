{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Vehicule</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ entity.matricule }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ entity.marque }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ entity.capacite }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('vehicule') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('vehicule_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
